PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=multidrive1234
DB_NAME=campusassist

JWT_SECRET=change_this_to_a_local_secret
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
# Only set true outside production if you need the API to return reset tokens directly.
RETURN_PASSWORD_RESET_TOKEN=false

